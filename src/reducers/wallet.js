const INITIAL_STATE = {
  expenses: [],
};
const walletReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  case value:

    break;

  default:
    return state;
  }
};

export default walletReducer;
